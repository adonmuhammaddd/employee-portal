import { RupiahMaskDirective } from './rupiah-mask.directive';

describe('RupiahMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new RupiahMaskDirective();
    expect(directive).toBeTruthy();
  });
});
